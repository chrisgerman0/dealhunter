# Optional — enables Mapbox GL map on Explore and deal detail mini-map.
# Without it, a schematic fallback map is shown.
NEXT_PUBLIC_MAPBOX_TOKEN=

# Data source factory (prototype uses mock)
DATA_SOURCE=mock
